<%= form_for(@todo_item) do |f| %>
  <% if @todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
      <% @todo_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description, :class=> "submit3" %><br>
    <%= f.text_field :description, :class=> "submit2" %>
  </div>
  <div class="actions">
    <%= f.submit 'submit task', :class=> "submit" %>
  </div>
<% end %>


